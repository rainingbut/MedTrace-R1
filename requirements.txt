# MEDTRACE-R1 stage 0 baseline environment.
# Platform: Linux x86_64; Python 3.12 is recommended.
#
# vLLM owns the compatible torch/torchvision/transformers/CUDA Python stack.
# Do not add separate pins for those packages here. On GPU hosts install with:
#   uv pip install -r requirements.txt --torch-backend=auto
vllm==0.24.0

# Evaluation/configuration dependency (kept in sync with requirements/eval.txt).
PyYAML==6.0.2
